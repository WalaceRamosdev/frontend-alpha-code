---
import Card from "../ui/Card.astro";
---

<section id="services" class="bg-alt">
    <div class="container">
        <div class="section-header">
            <h2 class="fade-up-services">
                O que <span class="text-primary">Entregamos</span>
            </h2>
            <p class="fade-up-services delay-1 section-desc">
                Soluções digitais desenhadas estrategicamente para gerar
                negócios.
            </p>
        </div>
        <div class="services-list">
            <Card variant="service" class="service-card-anim">
                <div class="service-icon">🚀</div>
                <div class="service-info">
                    <h3>Landing Page de Alta Conversão</h3>
                    <p>
                        Página única, focada 100% em transformar visitante em
                        cliente. Ideal para médicos e advogados que fazem
                        tráfego pago (Ads).
                    </p>
                    <span class="service-tag">Foco em Vendas</span>
                </div>
            </Card>
            <Card variant="service" class="service-card-anim">
                <div class="service-icon">🏛️</div>
                <div class="service-info">
                    <h3>Site Institucional Premium</h3>
                    <p>
                        Múltiplas páginas (Home, Sobre, Serviços, Blog) para
                        quem constrói uma marca sólida. Transmite robustez,
                        confiança e tradição.
                    </p>
                    <span class="service-tag">Foco em Autoridade</span>
                </div>
            </Card>
            <Card variant="service" class="service-card-anim">
                <div class="service-icon">🔍</div>
                <div class="service-info">
                    <h3>SEO e Posicionamento Google</h3>
                    <p>
                        Otimização técnica para que seu nome apareça quando
                        alguém pesquisar "advogado em [sua cidade]" ou "clínica
                        [x]".
                    </p>
                    <span class="service-tag">Foco em Tráfego Gratuito</span>
                </div>
            </Card>
        </div>
    </div>
</section>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    document.addEventListener("DOMContentLoaded", () => {
        const section = document.getElementById("services");

        if (section) {
            gsap.from(".fade-up-services", {
                scrollTrigger: {
                    trigger: ".section-header",
                    start: "top 80%",
                },
                y: 40,
                opacity: 0,
                duration: 0.8,
                stagger: 0.2,
                ease: "power2.out",
            });

            gsap.from(".service-card-anim", {
                scrollTrigger: {
                    trigger: ".services-list",
                    start: "top 75%",
                },
                y: 60,
                opacity: 0,
                duration: 0.8,
                stagger: 0.2,
                ease: "power2.out",
            });
        }
    });
</script>

<style>
    #services {
        padding: 100px 0;
    }
    .section-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    .section-desc {
        max-width: 600px;
        margin: 1rem auto;
        color: var(--color-text-secondary);
    }
    .services-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 30px;
    }

    .service-info {
        text-align: center;
        flex-grow: 1;
        display: flex;
        flex-direction: column;
    }
    .service-icon {
        font-size: 3rem;
        margin-bottom: 1.5rem;
        text-align: center;
    }
    .service-info h3 {
        margin-bottom: 1rem;
        font-size: 1.4rem;
        color: var(--color-heading);
    }
    .service-info p {
        flex-grow: 1;
        margin-bottom: 1.5rem;
    }
    .service-tag {
        display: inline-block;
        padding: 4px 12px;
        background: rgba(var(--color-primary-rgb), 0.1);
        color: var(--color-primary);
        font-size: 0.8rem;
        font-weight: 600;
        border-radius: 20px;
        margin-top: auto;
        align-self: center;
    }
</style>
