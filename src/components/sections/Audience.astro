---
import Card from "../ui/Card.astro";
---

<section id="audience" class="bg-alt">
    <div class="container">
        <div class="section-header">
            <h2 class="fade-up-audience">
                Para quem é a <br /><span class="text-primary">Alpha Code</span
                >?
            </h2>
            <p class="fade-up-audience delay-1 section-desc">
                Especialistas em posicionamento digital para quem vende
                conhecimento, imagem ou serviço premium.
            </p>
        </div>
        <div class="audience-grid">
            <Card variant="default" class="audience-card">
                <div class="card-icon">🩺</div>
                <h3>Saúde & Bem-estar</h3>
                <p class="audience-role">Médicos, Psicólogos, Nutricionistas</p>
                <ul class="audience-benefits">
                    <li>Pare de brigar por preço de consulta.</li>
                    <li>
                        Atraia pacientes particulares que valorizam seu
                        atendimento.
                    </li>
                    <li>
                        Transmita segurança e profissionalismo antes do
                        agendamento.
                    </li>
                </ul>
            </Card>
            <Card variant="default" class="audience-card">
                <div class="card-icon">⚖️</div>
                <h3>Jurídico & Fiscal</h3>
                <p class="audience-role">Advogados, Contadores, Consultores</p>
                <ul class="audience-benefits">
                    <li>Domine a primeira página do Google na sua região.</li>
                    <li>
                        Passe a imagem de um escritório consolidado e
                        experiente.
                    </li>
                    <li>
                        Gere leads qualificados sem depender de indicações
                        aleatórias.
                    </li>
                </ul>
            </Card>
            <Card variant="default" class="audience-card">
                <div class="card-icon">📸</div>
                <h3>Criativos & Arquitetos</h3>
                <p class="audience-role">Fotógrafos, Arquitetos, Designers</p>
                <ul class="audience-benefits">
                    <li>
                        Um portfólio que valoriza cada detalhe do seu trabalho.
                    </li>
                    <li>Layout minimalista que coloca sua obra em destaque.</li>
                    <li>
                        Venda o "Sonho" e justifique cobrar o valor que você
                        merece.
                    </li>
                </ul>
            </Card>
            <Card variant="default" class="audience-card">
                <div class="card-icon">💼</div>
                <h3>Empresas & Serviços</h3>
                <p class="audience-role">Clínicas, Agências, Prestadores</p>
                <ul class="audience-benefits">
                    <li>Automatize a apresentação do seu serviço.</li>
                    <li>Receba pedidos de orçamento detalhados 24h por dia.</li>
                    <li>
                        Mostre que sua empresa é grande, organizada e confiável.
                    </li>
                </ul>
            </Card>
        </div>
    </div>
</section>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    document.addEventListener("DOMContentLoaded", () => {
        const section = document.getElementById("audience");

        if (section) {
            gsap.from(".fade-up-audience", {
                scrollTrigger: {
                    trigger: "#audience",
                    start: "top 80%",
                },
                y: 30,
                opacity: 0,
                duration: 0.8,
                stagger: 0.1,
            });

            gsap.from(".audience-card", {
                scrollTrigger: {
                    trigger: ".audience-grid",
                    start: "top 80%",
                },
                y: 50,
                opacity: 0,
                duration: 0.8,
                stagger: 0.15,
                ease: "power2.out",
            });
        }
    });
</script>

<style>
    #audience {
        padding: 100px 0;
    }
    .section-desc {
        max-width: 600px;
        margin: 1rem auto 3rem;
        color: var(--color-text-secondary);
        text-align: center;
    }
    .audience-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 20px; /* Slightly tighter gap for mobile */
        margin-top: 40px;
    }

    @media (max-width: 900px) {
        .audience-grid {
            /* Keep it as grid, 2 columns */
            grid-template-columns: repeat(2, 1fr);
            gap: 15px; /* Tighter gap for small screens */
            padding-bottom: 0;
            display: grid;
            overflow-x: visible;
        }

        .audience-card {
            /* Remove min-width to allow shrinking to column width */
            min-width: 0;
            padding: 1.5rem; /* Reduce padding slightly */
        }
    }

    @media (max-width: 480px) {
        /* On VERY small screens, 2 columns might be unreadable. 
           However, user asked for "2 columns". 
           I will try to respect that, but if it breaks content, I'll fallback or adjust text size. 
           Let's keep 2 columns but reduce side padding significantly. */
        .audience-grid {
            gap: 10px;
        }
        .audience-card {
            padding: 1rem;
        }
    }
    .section-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    .audience-card {
        padding: 30px;
        height: 100%;
        display: flex;
        flex-direction: column;
        transition: all 0.3s ease;
        border-radius: 24px; /* Softer, friendlier corners */
        background: rgba(255, 255, 255, 0.03); /* Subtle depth */
        border: 1px solid rgba(255, 255, 255, 0.05);
    }
    .audience-card:hover {
        transform: translateY(-5px);
        background: rgba(255, 255, 255, 0.05);
        border-color: var(--color-primary);
    }
    .card-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }
    .audience-card h3 {
        margin-bottom: 0.5rem;
        color: var(--color-heading);
        font-size: 1.3rem; /* Slightly smaller for better fit */
        font-weight: 700;
        line-height: 1.3;
    }
    .audience-role {
        color: var(--color-primary);
        font-size: 0.9rem;
        font-weight: 500; /* Less aggressive than 600 */
        margin-bottom: 1.2rem;
        /* Removed text-transform: uppercase for a friendlier, space-saving look */
        line-height: 1.4;
    }
    .audience-benefits {
        list-style: none;
        padding: 0;
        margin: 0;
        flex-grow: 1;
    }
    .audience-benefits li {
        position: relative;
        padding-left: 20px;
        margin-bottom: 10px;
        font-size: 0.9rem; /* optimized for reading */
        color: var(--color-text-container); /* Softer white */
        line-height: 1.5;
    }
    .audience-benefits li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--color-primary);
        font-weight: bold;
    }

    @media (max-width: 900px) {
        .audience-card {
            padding: 20px 15px; /* Compact padding for mobile */
            border-radius: 20px;
        }
        .audience-card h3 {
            font-size: 1.15rem; /* Adapt title size */
        }
        .audience-role {
            font-size: 0.85rem;
            margin-bottom: 1rem;
        }
        .card-icon {
            font-size: 2rem;
        }
        .audience-benefits li {
            font-size: 0.85rem; /* Readable but compact */
            line-height: 1.4;
        }
    }
</style>
