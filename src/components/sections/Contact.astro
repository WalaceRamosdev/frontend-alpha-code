---
import Button from "../ui/Button.astro";
---

<section id="contact">
    <div class="container">
        <div class="cta-card fade-up">
            <div class="cta-content text-center">
                <h2>Pronto para dominar seu mercado?</h2>
                <p>
                    A diferença entre você e o líder do seu nicho é a estratégia
                    digital.
                </p>
                <div class="actions">
                    <Button
                        href="https://wa.me/5521999064502"
                        variant="primary"
                        size="lg"
                        target="_blank"
                        class="glow-effect"
                    >
                        <span class="btn-content">Iniciar Projeto Agora</span>
                    </Button>
                </div>
            </div>
            <div class="glow-bg"></div>
        </div>
    </div>
</section>

<style>
    #contact {
        padding: 50px 0 100px;
    }

    .cta-card {
        background: linear-gradient(
            145deg,
            rgba(20, 20, 20, 0.9),
            rgba(10, 10, 10, 0.95)
        );
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 30px;
        padding: 60px 20px;
        position: relative;
        overflow: hidden;
        text-align: center;
        box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.5);
    }

    .cta-content {
        position: relative;
        z-index: 2;
    }

    .glow-bg {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 100%;
        height: 100%;
        background: radial-gradient(
            circle,
            rgba(138, 28, 38, 0.2) 0%,
            transparent 70%
        );
        z-index: 1;
        pointer-events: none;
    }

    h2 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        background: var(--gradient-primary);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
        display: inline-block;
    }

    p {
        font-size: 1.25rem;
        color: var(--color-text-secondary);
        margin-bottom: 2rem;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    @media (max-width: 768px) {
        h2 {
            font-size: 1.8rem;
        }
        .cta-card {
            padding: 40px 20px;
        }
    }
</style>
