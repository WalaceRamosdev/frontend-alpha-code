---

---

<div id="social-proof-popup" class="social-proof-container hidden">
    <div class="sp-card">
        <div class="sp-icon">
            <span class="pulse-icon"></span>
            🚀
        </div>
        <div class="sp-content">
            <p class="sp-text" id="sp-message"></p>
            <span class="sp-time">há poucos segundos</span>
        </div>
        <button class="sp-close" id="sp-close">&times;</button>
    </div>
</div>

<style>
    .social-proof-container {
        position: fixed;
        bottom: 30px;
        left: 30px;
        z-index: 999;
        transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        pointer-events: none;
        opacity: 0;
        transform: translateY(20px) scale(0.9);
    }

    .social-proof-container.active {
        opacity: 1;
        transform: translateY(0) scale(1);
        pointer-events: auto;
    }

    .social-proof-container.hidden {
        display: none;
    }

    .sp-card {
        background: rgba(10, 10, 10, 0.9);
        border: 1px solid rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(15px);
        padding: 12px 20px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        gap: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        min-width: 250px;
        max-width: 320px;
        position: relative;
    }

    .sp-icon {
        width: 40px;
        height: 40px;
        background: rgba(138, 28, 38, 0.15);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
        position: relative;
    }

    .pulse-icon {
        position: absolute;
        top: -2px;
        right: -2px;
        width: 8px;
        height: 8px;
        background: #22c55e;
        border-radius: 50%;
        box-shadow: 0 0 8px #22c55e;
    }

    .pulse-icon::after {
        content: "";
        position: absolute;
        inset: -4px;
        border-radius: 50%;
        border: 1px solid #22c55e;
        animation: pulse 1.5s infinite;
    }

    @keyframes pulse {
        0% {
            transform: scale(1);
            opacity: 1;
        }
        100% {
            transform: scale(3);
            opacity: 0;
        }
    }

    .sp-content {
        display: flex;
        flex-direction: column;
        gap: 2px;
    }

    .sp-text {
        color: white;
        font-size: 0.9rem;
        font-weight: 500;
        margin: 0;
        line-height: 1.2;
    }

    .sp-time {
        color: rgba(255, 255, 255, 0.4);
        font-size: 0.75rem;
    }

    .sp-close {
        position: absolute;
        top: 5px;
        right: 8px;
        background: none;
        border: none;
        color: rgba(255, 255, 255, 0.3);
        cursor: pointer;
        font-size: 1.2rem;
        line-height: 1;
        padding: 0;
    }

    .sp-close:hover {
        color: white;
    }

    @media (max-width: 600px) {
        .social-proof-container {
            bottom: 20px;
            left: 20px;
            right: 20px;
        }
        .sp-card {
            max-width: none;
            width: 100%;
        }
    }
</style>

<script>
    const messages = [
        "Dr. Marcelo de SP acabou de solicitar um orçamento.",
        "Dra. Juliana do RJ iniciou um projeto VIP.",
        "Escritório Silva & Associados vinculou um novo site.",
        "Ricardo M. acaba de contratar o plano PRO.",
        "Dra. Fernanda de MG solicitou uma melhoria.",
        "Clínica Sorriso de SC iniciou um novo projeto.",
        "Advogado Carlos H. de Brasília solicitou orçamento.",
        "Portal de Arquitetura acabou de assinar o plano VIP.",
        "Dra. Beatriz do RS vinculou seu site agora.",
        "Felipe de Curitiba contratou melhoria PRO.",
        "Escritório Jurídico T. iniciou sua presença digital.",
    ];

    const popup = document.getElementById("social-proof-popup");
    const messageEl = document.getElementById("sp-message");
    const closeBtn = document.getElementById("sp-close");

    if (popup && messageEl && closeBtn) {
        let timer: any;

        const showPopup = () => {
            const randomMsg =
                messages[Math.floor(Math.random() * messages.length)];
            messageEl.innerText = randomMsg;

            popup.classList.remove("hidden");
            setTimeout(() => popup.classList.add("active"), 100);

            // Auto hide after 5 seconds
            timer = setTimeout(hidePopup, 6000);
        };

        const hidePopup = () => {
            popup.classList.remove("active");
            setTimeout(() => popup.classList.add("hidden"), 500);

            // Wait 15-25 seconds before showing again
            const nextTime = 15000 + Math.random() * 15000;
            setTimeout(showPopup, nextTime);
        };

        closeBtn.addEventListener("click", () => {
            clearTimeout(timer);
            hidePopup();
        });

        // Start the first one after 10 seconds
        setTimeout(showPopup, 10000);
    }
</script>
