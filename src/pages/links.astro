---
import BaseLayout from "../layouts/BaseLayout.astro";

const links = [
    {
        title: "Solicitar Orçamento Grátis",
        url: "/#contact",
        icon: "🚀",
        highlight: true,
    },
    {
        title: "Falar no WhatsApp",
        url: "https://wa.me/5521999064502",
        icon: "💬",
    },
    {
        title: "Ver Portfólio de Elite",
        url: "/#projects",
        icon: "📂",
    },
    {
        title: "Área do Cliente (Acessar)",
        url: "/dashboard",
        icon: "🔐",
    },
    {
        title: "Nossas Soluções",
        url: "/#services",
        icon: "✨",
    },
];

export const prerender = true;
---

<BaseLayout
    title="Links Importantes | Alpha Code"
    showFooter={false}
    showHeader={false}
>
    <div class="links-page">
        <div class="ambient-glow"></div>

        <div class="links-container">
            <header class="links-header">
                <div class="logo-wrapper">
                    <img
                        src="/assets/logo.svg"
                        alt="Alpha Code"
                        class="links-logo"
                    />
                </div>
                <h1>Alpha Code</h1>
                <p>
                    Transformando sua presença digital com autoridade e
                    tecnologia.
                </p>
            </header>

            <div class="links-list">
                {
                    links.map((link) => (
                        <a
                            href={link.url}
                            class={`link-item ${link.highlight ? "highlight" : ""}`}
                        >
                            <span class="link-icon">{link.icon}</span>
                            <span class="link-title">{link.title}</span>
                            <span class="link-arrow">→</span>
                        </a>
                    ))
                }
            </div>

            <footer class="links-footer">
                <p>
                    © {new Date().getFullYear()} Alpha Code - High Performance Web
                </p>
            </footer>
        </div>
    </div>
</BaseLayout>

<style>
    .links-page {
        min-height: 100vh;
        background: #050505;
        display: flex;
        justify-content: center;
        padding: 60px 20px;
        position: relative;
        overflow: hidden;
    }

    .ambient-glow {
        position: absolute;
        top: -10%;
        left: 50%;
        transform: translateX(-50%);
        width: 100%;
        height: 400px;
        background: radial-gradient(
            circle,
            rgba(138, 28, 38, 0.15) 0%,
            transparent 70%
        );
        pointer-events: none;
    }

    .links-container {
        width: 100%;
        max-width: 450px;
        position: relative;
        z-index: 10;
        text-align: center;
    }

    .links-header {
        margin-bottom: 40px;
    }

    .logo-wrapper {
        width: 80px;
        height: 80px;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 20px;
        backdrop-filter: blur(10px);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    }

    .links-logo {
        width: 45px;
        height: 45px;
    }

    .links-header h1 {
        font-family: "Outfit", sans-serif;
        font-size: 1.8rem;
        font-weight: 800;
        color: white;
        margin-bottom: 10px;
    }

    .links-header p {
        color: rgba(255, 255, 255, 0.6);
        font-size: 0.95rem;
        line-height: 1.5;
    }

    .links-list {
        display: flex;
        flex-direction: column;
        gap: 16px;
    }

    .link-item {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.08);
        padding: 18px 24px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        text-decoration: none;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
    }

    .link-item:hover {
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(255, 255, 255, 0.2);
        transform: translateY(-3px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
    }

    .link-item.highlight {
        background: var(--color-primary, #8a1c26);
        border-color: rgba(255, 255, 255, 0.2);
        box-shadow: 0 10px 30px rgba(138, 28, 38, 0.3);
    }

    .link-item.highlight:hover {
        background: #a1212d;
        box-shadow: 0 15px 40px rgba(138, 28, 38, 0.5);
    }

    .link-icon {
        font-size: 1.4rem;
        margin-right: 15px;
    }

    .link-title {
        color: white;
        font-weight: 600;
        font-size: 1rem;
        flex-grow: 1;
        text-align: left;
    }

    .link-arrow {
        color: rgba(255, 255, 255, 0.3);
        font-size: 1.2rem;
        transition: 0.3s;
    }

    .link-item:hover .link-arrow {
        color: white;
        transform: translateX(5px);
    }

    .links-footer {
        margin-top: 50px;
        color: rgba(255, 255, 255, 0.3);
        font-size: 0.8rem;
    }

    @media (max-width: 480px) {
        .links-page {
            padding: 40px 15px;
        }
    }
</style>
